get:
  tags:
    - UsagePoint
  summary: Retrieve a Subscription Usage Point by its ID [FB_32]
  description: >-
    Returns an Atom entry containing the Usage Point identified by the
    given ID within the specified Subscription. Requires a valid
    data_custodian_access_token or access_token.
  operationId: getUsagePointBySubscription
  parameters:
    - name: subscriptionId
      in: path
      description: The Subscription's ID.
      required: true
      schema:
        type: string
    - name: usagePointId
      in: path
      description: ID of the Usage Point.
      required: true
      schema:
        type: string
    - $ref: '../components/parameters/published-max.yaml'
    - $ref: '../components/parameters/published-min.yaml'
    - $ref: '../components/parameters/updated-max.yaml'
    - $ref: '../components/parameters/updated-min.yaml'
    - $ref: '../components/parameters/max-results.yaml'
    - $ref: '../components/parameters/start-index.yaml'
    - $ref: '../components/parameters/depth.yaml'
  security:
    - oauth2: []
  responses:
    '200':
      $ref: '../components/responses/AtomEntry_UsagePoint.yaml'
    '202':
      description: Accepted - The request has been accepted for processing.
    '400':
      description: Bad Request - The request was malformed or contained invalid parameters.
    '403':
      description: Forbidden - The access token does not have sufficient scope.
